<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kosta.myproject.model.mapper.StoreMapper">
	<resultMap type="StoreDTO" id="storeListRM">
      <result column="post_id" property="boardDTO.postId" />      
   </resultMap>
	<insert id="storeWrite" parameterType="StoreDTO">
	insert into store(post_id, price, place, locinfo_x,locinfo_y)
		values(#{boardDTO.postId},#{price},#{place},#{locinfoX},#{locinfoY})
	</insert>
	<select id="getStoreDetail" resultType="StoreDTO" parameterType="int">
		select price, place, locinfo_x, locinfo_y
		from store
		where post_id=#{postId}
	</select>
	<select id="getAllList" resultType="StoreDTO" resultMap="storeListRM">
		select post_id, locinfo_x, locinfo_y
		from store
	</select>
</mapper>